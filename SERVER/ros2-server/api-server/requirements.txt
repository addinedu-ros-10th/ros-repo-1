# FastAPI 및 웹 서버
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP 클라이언트
httpx==0.25.2

# ROS2 (선택적, 로봇 환경에서만 필요)
# rclpy는 ROS2 환경에 설치되어 있어야 함
# 설치 방법: sudo apt install ros-<distro>-rclpy

# 로깅
python-json-logger==2.0.7

# Redis (선택적)
redis==5.0.1

# 유틸리티
python-dotenv==1.0.0

# ROS2 의존성 (ROS2 패키지가 필요로 하는 패키지)
numpy>=1.20.0  # ROS2 메시지 타입 및 인터페이스에서 사용

# 마크다운 처리
markdown>=3.4.0  # 마크다운을 HTML로 변환
Pygments>=2.14.0  # 코드 하이라이팅 (codehilite 확장용)

# ROS2 (호스트에 설치된 ROS2 사용, requirements.txt에는 명시만)
# 실제 설치: sudo apt install ros-<distro>-rclpy
# 또는 호스트의 ROS2 환경에서 실행

